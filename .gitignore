# Dependencies
/web/node_modules
/server/vendor

# Build output
/web/dist
/server/tmp
# 上传暂存(媒体上传运行时产物)
/server/uploads

# OS
.DS_Store
Thumbs.db

# Local env overrides (secrets)
.env.local
.env.*.local
# Server runtime env (may contain secrets)
/server/.env

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
server/log/*

# IDE / Editor
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Binary files
*.exe

# Database files
*.db
*.sqlite
*.sqlite3

# Claude Code
.claude/worktrees/
.claude/scheduled_tasks.json

# Temp / local-only
/skill*
settings.local.json
.local/
Soy*
worktrees/
.codegraph/

# Superpowers（AI 工作流产物：spec / plan / sdd 统一存放于此，不入库）
.superpowers/

# 生产部署机密（docker-prod）：仅 .env 入库被忽略
# config.yaml 是静态入库配置（密码由 .env 经 env 覆盖，本身不含真实密钥），故入库
deploy/docker-prod/.env
