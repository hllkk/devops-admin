# 敏感配置环境变量清单(后端不读 .env 文件,由部署层注入)。
#
# 用法:复制为 server/.env(根 .gitignore 已忽略 /server/.env,不会入库),或用作
# systemd EnvironmentFile / docker-compose env_file / k8s ConfigMap 的模板。
#
# DB/Redis/Mongo 等连接配置由初始化向导(/init/initdb)回写 config.yaml 管理,
# 每个环境各自初始化,无需此处注入。仅 JWT 签名密钥(不进向导、泄露可伪造 token)
# 需环境变量注入。

# JWT 签名密钥(生产必填,建议 32+ 字节随机串: openssl rand -hex 32)
OPS_JWT_SIGNING_KEY=
