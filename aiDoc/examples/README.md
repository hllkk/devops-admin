# 示例层 (examples)

> 讲解型示例：告诉 AI 每层按什么标准**组织**代码，**不是逐字复制**。
> 新增某层文件前，先读对应示例再实现。
> TODO：随项目补充。

## 计划
- `backend/`：后端分层写法示例（model/request/service/api/router/enter.go/plugin.go），见 `backend/README.md`
- `frontend/`：Soybean 范例（一个 api + view + 自动路由 + i18n）
- `plugin/`：前后端插件范例
