# 示例层 (examples)

> 讲解型示例：告诉 AI 每层按什么标准**组织**代码，**不是逐字复制**。
> 新增某层文件前，先读对应示例再实现。

## 现状
- `backend/`：✅ 后端分层写法示例已齐（`README` + model/request/service/api/router/enter-go/plugin-go），见 `backend/README.md`
- `frontend/`：⏳ 待补 Soybean 范例（一个 api + view + 自动路由 + i18n）；过渡期参考真实模块 `web/src/views/_admin/system/dept/`（index + operate-drawer + search 三件套）与 `frontend-rules.md` §9
- `plugin/`：⏳ 待补前后端插件范例
